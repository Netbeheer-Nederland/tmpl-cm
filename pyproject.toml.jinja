[project]
name = "{{ name }}"
requires-python = ">=3.12,<4"
dependencies = [
    "linkml >= 1.8.4, < 1.9",
    "linkml-runtime >= 1.8.1, < 1.9",
]

version = "1.0.0"  # Required version which is not used by us.
